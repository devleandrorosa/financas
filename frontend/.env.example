# URL do backend Laravel em produção (sem barra final).
# Em dev, deixe vazio: o Vite proxy de /api → http://nginx:80 já cuida disso.
# Em produção (Vercel), defina: VITE_API_URL=https://seu-backend.up.railway.app
VITE_API_URL=
